{
  "name": "usb-site-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "USB Site - Monorepo with Sanity CMS and Next.js",
  "scripts": {
    "dev:web": "cd web && npm run dev",
    "dev:sanity": "cd sanity && npm run dev",
    "build:web": "cd web && npm run build",
    "build:sanity": "cd sanity && npm run build",
    "install:all": "cd sanity && npm install && cd ../web && npm install"
  },
  "workspaces": [
    "sanity",
    "web"
  ],
  "engines": {
    "node": ">=18.17.0",
    "npm": ">=9.0.0"
  }
}
